{
   "configure": null,
   "dependencies": [],
   "description": "Web content management system",
   "environment": [
      { "variable": "WORDPRESS_ROOT_PASSWORD", "preset": "$(random_password)" },
      { "variable": "WORDPRESS_DB_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
      { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "wordpress"
}
