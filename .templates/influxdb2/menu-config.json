{
   "configure": null,
   "dependencies": [],
   "description": "Time-series DBMS v2 (not recommended)",
   "environment": [
      { "variable": "INFLUXDB2_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
      { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "influxdb2"
}
