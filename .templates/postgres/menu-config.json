{
   "configure": null,
   "dependencies": [],
   "description": "PostgreSQL relational DBMS",
   "environment": [
      { "variable": "POSTGRES_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
      { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "postgres"
}
