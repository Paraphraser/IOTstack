{
   "configure": null,
   "dependencies": [],
   "description": "Graphical user interface to PostgreSQL",
   "environment": [
      { "variable": "PGADMIN_DEFAULT_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
      { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "pgadmin4"
}
