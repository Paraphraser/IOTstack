{
   "configure": null,
   "dependencies": [],
   "description": "Reverse-proxy manager",
   "environment": [
      { "variable": "NGINX_INITIAL_ADMIN_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
     { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "nginx"
}
