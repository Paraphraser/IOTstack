{
   "configure": null,
   "dependencies": [],
   "description": "Ad-blocker with DNS and DHCP services",
   "environment": [
      { "variable": "PIHOLE_ADMIN_PASSWORD", "preset": "$(random_password)" }
   ],
   "install": [
      { "template": "service.yml", "service": "service.yml", "tracked": true }
   ],
   "service": "pihole6"
}
